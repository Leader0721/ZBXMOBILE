<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:icon="@mipmap/menu_creat_blog"
        android:title="选项"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/mMenuCollect"
                android:icon="@mipmap/bg_collect_no"
                android:title="收藏"
                app:showAsAction="always" />

            <item
                android:id="@+id/mSelectMessage"
                android:title="删除"
                android:visible="false"
                app:showAsAction="never" />

            <item
                android:id="@+id/mMenuSick"
                android:title="取消置顶"
                android:visible="false"
                app:showAsAction="never" />
        </menu>
    </item>

</menu>